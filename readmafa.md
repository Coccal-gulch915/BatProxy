باشه! متوجه شدم. شما یک README فارسی می‌خواهید که **خیلی روان و خودمونی** باشه، نه انگلیسی و نه خیلی خشک و رسمی. دقیقاً مثل این که یه دوست داره بهتون آموزش بده. بفرمایید:

---

# 🦇 BatProxy - راهنمای کامل و ساده

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)

**BatProxy** یه ابزار باحاله که بهت کمک میکنه از طریق کارگرهای کلودفلر (Cloudflare Workers) یک تونل پروکسی بسازی. یعنی هر درخواستی که از مرورگر یا برنامه‌هات می‌ری، از طریق این تونل به مقصد می‌رسه. 

> **📌 یه نکته مهم امنیتی**: برای ثبت‌نام در کلودفلر، بهتره از **ایمیل یکبار مصرف** استفاده کنی (مثل پروتون‌میل یا سرویس‌های مشابه). این کار حریم خصوصیت رو بیشتر میکنه.

---

## 📑 فهرست مطالب

- [این چیزه دقیقاً چیه؟](#این-چیزه-دقیقاً-چیه)
- [چرا باید ازش استفاده کنم؟](#چرا-باید-ازش-استفاده-کنم)
- [ساختار کلی کارش به چه شکلیه؟](#ساختار-کلی-کارش-به-چه-شکلیه)
- [مرحله به مرحله: نصب و راه‌اندازی](#مرحله-به-مرحله-نصب-و-راه‌اندازی)
  - [قسمت ۱: ساخت اکانت کلودفلر و دیپلوی ورکر](#قسمت-۱-ساخت-اکانت-کلودفلر-و-دیپلوی-ورکر)
  - [قسمت ۲: نصب و راه‌اندازی کلاینت پایتون](#قسمت-۲-نصب-و-راه‌اندازی-کلاینت-پایتون)
- [چطور ازش استفاده کنم؟](#چطور-ازش-استفاده-کنم)
  - [تنظیم پروکسی در مرورگر](#تنظیم-پروکسی-در-مرورگر)
  - [استفاده با curl](#استفاده-با-curl)
  - [چند تا ورکر داشته باشم بهتره؟](#چند-تا-ورکر-داشته-باشم-بهتره)
- [داشبورد و مانیتورینگ](#داشبورد-و-مانیتورینگ)
  - [داشبورد ترمینال](#داشبورد-ترمینال)
  - [داشبورد وب](#داشبورد-وب)
- [تنظیمات پیشرفته (اختیاری)](#تنظیمات-پیشرفته-اختیاری)
- [مشکلات رایج و راه حل‌ها](#مشکلات-رایج-و-راه-حل‌ها)
- [سوالات متداول](#سوالات-متداول)
- [مشارکت در پروژه](#مشارکت-در-پروژه)
- [مجوز](#مجوز)

---

## این چیزه دقیقاً چیه؟

خب، بذارین ساده بگم. فرض کنین یه سایت یا سرویسی هست که می‌خواین بهش دسترسی داشته باشین، ولی به هر دلیلی (تحریم، فیلترینگ، محدودیت جغرافیایی) نمی‌تونین مستقیم وصل بشین.

**BatProxy** به کمک شما میاد. یه پروکسی هوشمنده که:
- از **کارگرهای کلودفلر** استفاده میکنه (همون سرورهای رایگان کلودفلر)
- ترافیک شما رو از طریق یه تونل امن WebSocket هدایت میکنه
- اگر یکی از کارگرها خراب شد، به طور خودکار میره سراغ بعدی
- هم **HTTP** و هم **SOCKS5** رو پشتیبانی میکنه
- یه داشبورد قشنگ داره که وضعیت رو بهت نشون میده

**به عبارت دیگه:** یه پروکسی خودت می‌سازی که روی سرورهای کلودفلر اجرا میشه و هیچکس جز خودت بهش دسترسی نداره!

---

## چرا باید ازش استفاده کنم؟

خب، چند تا دلیل خوب داره:

✅ **رایگانه**: کلودفلر ورکر یه میزان مصرف رایگان داره که برای استفاده معمولی کافیه  
✅ **سریعه**: چون سرورهای کلودفلر همه جای دنیا هستن، همیشه نزدیک‌ترین سرور بهت رو انتخاب میکنه  
✅ **قابل اعتماده**: اگه یکی از کارگرها مشکل داشت، بقیه کار رو ادامه میدن  
✅ **امنه**: ارتباطات از طریق WebSocket با TLS رمزنگاری میشه  
✅ **ساده‌ست**: نصب و راه‌اندازیش خیلی راحته  
✅ **داشبورد داره**: میتونی همه چی رو آنلاین ببینی  

---

## ساختار کلی کارش به چه شکلیه؟

خب، بیایم یه تصویر ذهنی ازش بسازیم:

```
مرورگر یا برنامه‌های شما
         │
         ▼
┌─────────────────────┐
│  کلاینت BatProxy    │  ← روی کامپیوتر خودت اجرا میشه (پورت ۱۰۸۰)
│  (localhost:1080)   │
└─────────────────────┘
         │
         │  (اتصال از طریق WebSocket با رمزنگاری)
         ▼
┌─────────────────────┐
│  کارگر کلودفلر ۱    │  ← اینجا کد جاوااسکریپتت رو میزاری
└─────────────────────┘
         │
         ▼
┌─────────────────────┐
│  سایت یا سرویس هدف  │  ← مثلاً google.com یا هر جای دیگه
└─────────────────────┘
```

**داستان از این قراره:**

۱. برنامه‌های تو (مرورگر، curl، هرچی) به کلاینت BatProxy که روی کامپیوترت روشنه وصل میشن  
۲. کلاینت بهترین کارگر کلودفلر رو انتخاب میکنه  
۳. یه اتصال WebSocket امن با کارگر برقرار میشه  
۴. کارگر درخواست رو به سایت هدف میفرسته  
۵. پاسخ برمیگرده و بهت نمایش داده میشه  

**نکته جالبش اینجاست:** اگه یکی از کارگرها خراب بشه، کلاینت به طور خودکار میره سراغ کارگر بعدی. پس همیشه آنلاین هستی!

---

## مرحله به مرحله: نصب و راه‌اندازی

### قسمت ۱: ساخت اکانت کلودفلر و دیپلوی ورکر

**گام اول: برو تو سایت کلودفلر**

۱. برو به آدرس [https://workers.cloudflare.com/](https://workers.cloudflare.com/)  
۲. روی **"Sign up"** بزن  
۳. یه ایمیل وارد کن (پیشنهاد میکنم از یه ایمیل یکبار مصرف مثل پروتون‌میل استفاده کنی)  
۴. ثبت‌نام رو کامل کن  

**گام دوم: یه ورکر جدید بساز**

۱. از منوی سمت چپ، روی **"Compute"** یا **"Workers & Pages"** کلیک کن  
۲. روی دکمه **"Create application"** بزن  
۳. اگر دامنه نداری (معمولاً نداریم) روی **"Start with hello world"** کلیک کن  
۴. بعدش روی دکمه **"Deploy"** بزن  
۵. کلودفلر بهت یه ساب‌دامنه میده مثل `batmanpriv.workers.dev` یا `your-name.workers.dev`  
   **📌 این آدرس رو حتماً یادداشت کن!**

**گام سوم: کد ورکر رو جایگذاری کن**

۱. بعد از دیپلوی، روی دکمه **"Edit Code"** که در بالا سمت راسته کلیک کن  
۲. همه کدهای داخل ویرایشگر رو **پاک کن**  
۳. کد زیر رو **کپی** کن و جایگزین کن:

```javascript
// کل کد Worker رو از اینجا کپی کن
// https://github.com/batmanpriv/BatProxy/blob/main/worker/worker.js
```

۴. روی **"Save and Deploy"** کلیک کن  
۵. یه دقیقه صبر کن تا تغییرات اعمال بشه  

**✅ تبریک! ورکر تو آمادست!**

---

### قسمت ۲: نصب و راه‌اندازی کلاینت پایتون

**گام اول: پایتون رو نصب کن**

اگه پایتون نداری، از اینجا نصبش کن: [python.org](https://python.org/downloads/)

**گام دوم: ریپازیتوری رو کلون کن**

```bash
git clone https://github.com/batmanpriv/BatProxy.git
cd BatProxy
```

**گام سوم: کتابخونه‌های مورد نیاز رو نصب کن**

```bash
pip install websockets
```

اگه دوست داری داشبورد رنگی توی ترمینال داشته باشی:

```bash
pip install rich
```

اگه میخوای سرعت بیشتری داشته باشی:

```bash
pip install uvloop
```

**گام چهارم: تنظیمات رو انجام بده**

فایل `config.py` رو باز کن و آدرس ورکر خودت رو بذار:

```python
WORKERS = [
    {"url": "wss://your-name-domain.workers.dev", "password": "123456"},
]
```

**📌 یادت باشه:**
- `your-name-domain` رو با آدرسی که از کلودفلر گرفتی عوض کن
- پسورد رو هم همون `123456` بذار (یا هر چی که تو ورکر تنظیم کردی)

**گام پنجم: اجرا کن!**

```bash
python batproxy.py
```

اگه همه چی درست باشه، این پیغام رو میبینی:

```
🦇 Bat Proxy listening on 127.0.0.1:1080
  curl (HTTP)   : curl -x http://127.0.0.1:1080 https://example.com
  curl (SOCKS5) : curl -x socks5h://127.0.0.1:1080 https://example.com
```

**✅ تبریک! BatProxy روی کامپیوترت اجرا شده!**

---

## چطور ازش استفاده کنم؟

حالا که BatProxy روشن شده، میتونی ازش استفاده کنی.

### تنظیم پروکسی در مرورگر

**فایرفاکس:**
۱. برو به Settings → Network Settings  
۲. گزینه **"Manual proxy configuration"** رو انتخاب کن  
۳. HTTP Proxy رو بذار `127.0.0.1` و پورت `1080`  
۴. تیک گزینه **"Also use this proxy for HTTPS"** رو بزن  
۵. تیک گزینه **"Proxy DNS when using SOCKS v5"** رو بزن (برای SOCKS5)  

**کروم / اج:**
می‌تونی از افزونه‌های Proxy مثل SwitchyOmega استفاده کنی یا از خط فرمان:

```bash
chrome --proxy-server="http://127.0.0.1:1080"
```

**سیستم‌عامل (لینوکس/مک):**
```bash
export http_proxy="http://127.0.0.1:1080"
export https_proxy="http://127.0.0.1:1080"
```

**ویندوز:**
برو به Settings → Network & Internet → Proxy  
تنظیمات Manual Proxy رو روشن کن و آدرس `127.0.0.1` و پورت `1080` رو وارد کن.

### استفاده با curl

```bash
# پروکسی HTTP
curl -x http://127.0.0.1:1080 https://example.com

# پروکسی SOCKS5
curl -x socks5h://127.0.0.1:1080 https://example.com

# دانلود فایل از طریق پروکسی
curl -x http://127.0.0.1:1080 -O https://example.com/file.zip

# ارسال درخواست POST
curl -x http://127.0.0.1:1080 -X POST -d '{"key":"value"}' https://httpbin.org/post
```

### چند تا ورکر داشته باشم بهتره؟

بله! هرچی ورکر بیشتر باشه، پایداری و سرعت بالاتر میره. میتونی چند تا ورکر تو کلودفلر بسازی و همه رو به کلاینت اضافه کنی:

```python
WORKERS = [
    {"url": "wss://worker1.workers.dev", "password": "123456"},
    {"url": "wss://worker2.workers.dev", "password": "123456"},
    {"url": "wss://worker3.workers.dev", "password": "123456"},
]
```

کلاینت به طور خودکار بهترین ورکر رو انتخاب میکنه و اگه یکی خراب شد، میره سراغ بعدی.

---

## داشبورد و مانیتورینگ

BatProxy دو تا داشبورد باحال داره که وضعیت رو بهت نشون میده.

### داشبورد ترمینال

اگه کتابخونه `rich` رو نصب کرده باشی، توی ترمینال یه داشبورد رنگی و زنده میبینی که اینارو نشون میده:

- تعداد اتصالات فعال
- تعداد کل درخواست‌ها
- تعداد موفق و ناموفق
- وضعیت هر ورکر (سلامت، در حال استراحت، مشکل دار)
- تاخیر (RTT) هر ورکر
- امتیاز هر ورکر

### داشبورد وب

یه داشبورد وب هم هست که روی آدرس `http://127.0.0.1:8088` در دسترسه.

**ویژگی‌ها:**
- 📊 نمایش لحظه‌ای آمار
- 🟢 وضعیت رنگی هر ورکر
- 📈 نمودار امتیاز ورکرها
- 🔄 آپدیت خودکار هر ۱ ثانیه

**آدرس JSON API:** `http://127.0.0.1:8088/api/stats`

میتونی از این آدرس برای مانیتورینگ خودت استفاده کنی.

---

## تنظیمات پیشرفته (اختیاری)

اگه خواستی تنظیمات رو تغییر بدی، توی فایل `config.py` میتونی اینارو عوض کنی:

```python
# تنظیمات زمان‌بندی
CONNECT_TIMEOUT = 6          # چند ثانیه منتظر بمونه
MAX_ATTEMPTS_PER_REQUEST = 4 # چند بار تلاش کنه

# تنظیمات بافر
WRITE_BUFFER_MAX = 65536     # حداکثر اندازه بافر (۶۴ کیلوبایت)
WRITE_BUFFER_DELAY = 0.004   # تاخیر قبل از ارسال (۴ میلی‌ثانیه)

# تنظیمات تعادل بار
MAX_CONN_PER_WORKER = 150    # حداکثر اتصال همزمان به هر ورکر
HALF_OPEN_AFTER_FAILS = 3    # چند بار خطا قبل از قطع

# تنظیمات کش
DEST_CACHE_TTL = 600         # کش مقصد برای ۱۰ دقیقه
```

### تنظیمات پیشنهادی

**برای حداکثر پایداری:**
```python
MAX_ATTEMPTS_PER_REQUEST = 5
CONNECT_TIMEOUT = 8
HALF_OPEN_AFTER_FAILS = 2
HEALTH_CHECK_INTERVAL = 15
```

**برای حداکثر سرعت:**
```python
WRITE_BUFFER_MAX = 131072   # ۱۲۸ کیلوبایت
WRITE_BUFFER_DELAY = 0.002  # ۲ میلی‌ثانیه
MAX_CONN_PER_WORKER = 300
```

---

## مشکلات رایج و راه حل‌ها

### ۱. ارور اتصال

```
Error: ConnectionError: all workers failed
```

**راه حل:**
- اینترنت خودت رو چک کن
- آدرس ورکر رو درست وارد کردی؟
- `CONNECT_TIMEOUT` رو زیاد کن (به ۱۰ یا ۱۲)
- ورکر رو از داشبورد کلودفلر چک کن (روشن باشه؟)

### ۲. ارور احراز هویت

```
Error: invalid signature
```

**راه حل:**
- پسورد تو کلاینت و ورکر یکی باشه
- ساعت سیستم خودت رو تنظیم کن (حتماً NTP فعال باشه)
- متغیر `PASSWD` در کلودفلر رو چک کن

### ۳. اتصال WebSocket برقرار نمیشه

```
Error: WebSocket connection failed
```

**راه حل:**
- آدرس رو با `wss://` شروع کردی؟
- فایروال یا آنتی‌ویروس رو چک کن
- کلودفلر رو از طریق مرورگر چک کن (ببین بالا میاد؟)

### ۴. تاخیر بالا

```
Warning: Slow RTT detected
```

**راه حل:**
- ورکرهای دیگه رو امتحان کن (منطقه جغرافیایی متفاوت)
- `uvloop` رو نصب کن (`pip install uvloop`)
- `SLOW_RTT_MS` رو افزایش بده

### ۵. مصرف زیاد حافظه

```
MemoryError: Unable to allocate buffer
```

**راه حل:**
- `WRITE_BUFFER_MAX` رو کم کن
- `MAX_CONN_PER_WORKER` رو کم کن
- کلاینت رو ریستارت کن

---

## سوالات متداول

**س: چقدر ترافیک میتونم انتقال بدم؟**

ج: کلودفلر ورکر روزانه ۱۰۰٬۰۰۰ درخواست رایگان داره که برای استفاده معمولی کافیه. برای ترافیک بالا، پلن‌های پولی وجود داره.

**س: میشه ازش برای دانلود تورنت استفاده کرد؟**

ج: از نظر فنی بله، اما توصیه نمیکنم. کلودفلر محدودیت داره و ممکنه حساب‌ات رو مسدود کنن.

**س: برای بانکداری و اطلاعات حساس امن هست؟**

ج: بله، همه ترافیک از طریق TLS رمزنگاری میشه. ولی بازم توصیه میکنم برای اطلاعات خیلی حساس از VPN هم استفاده کنی.

**س: چقدر هزینه داره؟**

ج: ورکرهای کلودفلر پلن رایگان دارن. پلن‌های پولی از ۵ دلار در ماه شروع میشن.

**س: میشه چند تا کلاینت همزمان اجرا کنم؟**

ج: بله، میتونی روی پورت‌های مختلف چند تا کلاینت اجرا کنی.

**س: اگه همه ورکرها خراب بشن چی؟**

ج: کلاینت بهت ارور ۵۰۲ (Bad Gateway) میده و ورکرها میرن تو حالت استراحت (cooldown) تا دوباره راه‌بیفتن.

**س: IPv6 رو پشتیبانی میکنه؟**

ج: بله، هم کلاینت و هم کلودفلر ورکر از IPv6 پشتیبانی میکنن.

**س: چطور کد ورکر رو آپدیت کنم؟**

ج: برو تو داشبورد کلودفلر، کد رو ویرایش کن و Save and Deploy بزن. ۱-۲ دقیقه صبر کن تا اعمال بشه.

---


[برگشت به بالا](#batproxy)
---

این README فارسی رو چگونه دیدید؟ خواستید بخشی رو تغییر بدم یا چیز خاصی اضافه کنم؟
